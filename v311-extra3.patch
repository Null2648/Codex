--- a/app/src/main/java/kr/ac/library/inventory/MainActivity.java
+++ b/app/src/main/java/kr/ac/library/inventory/MainActivity.java
@@ -329,7 +329,7 @@
         LinearLayout.LayoutParams inputLp=new LinearLayout.LayoutParams(-1,-2);inputLp.setMargins(0,dp(7),0,0);root.addView(scanInput,inputLp);
 
         LinearLayout inputActions=row(); inputActions.addView(primaryButton("현재 입력 등록",v->submitEditText()),weight()); inputActions.addView(compactButton("직접 입력",v->showManualInputDialog()),weight()); root.addView(inputActions);
-        TextView scanHelp=text("스캐너 입력은 자동 판정합니다. 직접 입력은 오타 방지를 위해 등록 전 확인하며, 짧거나 의심스러운 번호는 확인 필요로 보류합니다.",12,false);scanHelp.setTextColor(C_SUB);scanHelp.setPadding(dp(3),0,dp(3),dp(3));root.addView(scanHelp);
+        TextView scanHelp=text("스캐너 입력은 자동 판정합니다. 버튼·붙여넣기·직접 입력은 사람 입력으로 검토하며, 짧거나 의심스러운 번호는 확인 필요로 보류합니다.",12,false);scanHelp.setTextColor(C_SUB);scanHelp.setPadding(dp(3),0,dp(3),dp(3));root.addView(scanHelp);
@@ -340,7 +340,7 @@
 
         LinearLayout quick=row();quick.addView(compactButton("스캔 목록",v->showTab(TAB_LIST)),weight());quick.addView(compactButton("확인 필요 기록",v->showIssues()),weight());root.addView(quick);
 
-        TextView recentTitle=text("최근 스캔",15,true);recentTitle.setPadding(dp(2),dp(12),0,dp(4));root.addView(recentTitle);
+        TextView recentTitle=text("최근 스캔 · 항목을 누르면 수정·삭제·이동",15,true);recentTitle.setPadding(dp(2),dp(12),0,dp(4));root.addView(recentTitle);
@@ -552,7 +552,7 @@
 
         LinearLayout reportGuide=card();reportGuide.setPadding(dp(15),dp(13),dp(15),dp(13));reportGuide.addView(text("결과 파일 내보내기",17,true));TextView reportGuideText=text("CSV·TXT·전체 보고서 ZIP 내보내기는 기록 탭에 모아 두었습니다. 현재 점검을 선택한 뒤 필요한 형식을 바로 저장할 수 있습니다.",14,false);reportGuideText.setTextColor(C_SUB);reportGuideText.setPadding(0,dp(5),0,dp(5));reportGuide.addView(reportGuideText);reportGuide.addView(compactButton("기록 탭으로 이동",v->showTab(TAB_RECORDS)));root.addView(reportGuide);
 
-        LinearLayout versionCard=card();versionCard.setPadding(dp(15),dp(13),dp(15),dp(13));versionCard.addView(text("앱 버전",17,true));TextView versionText=text(appVersionDisplay()+"\n기록 탭 전체 스크롤, 점검 카드 관리, 내보내기 이력 표시는 v3.10.0 이상에서 지원합니다.",14,false);versionText.setTextColor(C_SUB);versionText.setPadding(0,dp(5),0,0);versionCard.addView(versionText);root.addView(versionCard);
+        LinearLayout versionCard=card();versionCard.setPadding(dp(15),dp(13),dp(15),dp(13));versionCard.addView(text("앱 버전",17,true));TextView versionText=text(appVersionDisplay()+"\n직접 입력 재확인과 점검 기록 수정·삭제 기능은 v3.11.0 이상에서 지원합니다.",14,false);versionText.setTextColor(C_SUB);versionText.setPadding(0,dp(5),0,0);versionCard.addView(versionText);root.addView(versionCard);
